apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: keycloak-persistent-volume-claim
  namespace: development
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi