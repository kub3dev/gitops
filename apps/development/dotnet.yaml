apiVersion: apps/v1
kind: Deployment
metadata:
  name: dotnet-deployment
  namespace: development
spec:
  replicas: 5
  selector:
    matchLabels:
      name: dotnet
  template:
    metadata:
      labels:
        name: dotnet
    spec:
      containers:
        - name: nginx
          image: fclebinho/weatherforecast:1.0.0
          imagePullPolicy: Always
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: dotnet-service
  namespace: development
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 80
  selector:
    name: dotnet
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-issuer
  generation: 1
  name: dotnet-ingress
  namespace: development
spec:
  ingressClassName: nginx
  rules:
  - host: dotnet.kub3.dev
    http:
      paths:
      - backend:
          service:
            name: dotnet
            port:
              number: 80
        path: /
        pathType: Prefix
  tls:
  - hosts:
    - dotnet.kub3.dev
    secretName: le-dotnet-dev-cert-tls