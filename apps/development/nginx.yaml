apiVersion: apps/v1
kind: Deployment
metadata:
  name: site
  namespace: development
spec:
  replicas: 5
  selector:
    matchLabels:
      name: nginx-server
  template:
    metadata:
      labels:
        name: nginx-server
    spec:
      containers:
        - name: nginx
          image: nginx:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: nginx-server
  namespace: development
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 80
  selector:
    name: nginx-server
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-issuer
  generation: 1
  name: nginx-route
  namespace: development
spec:
  ingressClassName: nginx
  rules:
  - host: kub3.dev
    http:
      paths:
      - backend:
          service:
            name: nginx-server
            port:
              number: 80
        path: /
        pathType: Prefix
  tls:
  - hosts:
    - kub3.dev
    secretName: le-kub3-dev-cert-tls