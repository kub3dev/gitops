apiVersion: v1
kind: Service
metadata:
  name: identity-service
  namespace: development
spec:
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    name: identity